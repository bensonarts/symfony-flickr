{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% extends 'admin_base.html.twig' %}

{% block body %}
    <h2>Create Image</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('admin_image_index', { 'id' : category.id }) }}">Back</a>
{% endblock %}
